#include <stdio.h>

int fatorial(int n;
) {

   if ( (n <= 1) ) { 
      return 1;
   }
   else {
      return (n * fatorial((n - 1)));
   }
}
void print(int a;
int b;
string ch;
) {
   int i;
   printf("   %s   "   ,ch);
   while ( (i <= 5) ) { 
      printf("      %d      "      ,(a / b));
      i = (i + 1);
   }
   while ( (i <= 50) ) { 
      i = (i + 1);
   }
}
void teste() {

   int x;
   int y;
   int z;
   char re;
   string frase;
   int vetor[7];
scanf("%d %d %c ",&x,&y,&re);
print(0, 1, frase);
y = 10;
if ( (x >= y) ) { 
   printf("   %c   "   ,verdadeiro );
}
while ( (x < y) ) { 
   printf("enquanto isso    ");
   printf("Numa terra tao tao distante    \n");
}
}
